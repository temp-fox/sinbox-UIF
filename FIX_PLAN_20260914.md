# 修复方案（2026-09-14）

## 一、问题与根因

### 问题 1：节点选择突然不显示
- 现象：内核 `coreStatus=1`（closed），依赖 Clash API 的节点选择列表全部为空。
- 根因：订阅「江江」含 5 个 VLESS + `xhttp` 节点。sing-box 1.12.13 已移除 `xhttp`/`splithttp`
  传输类型（实测 `sing-box check` 报 `unknown transport type: xhttp`）。
  前端 `uif2singbox.js` 的 `Bound` 把 `transport.protocol` 原样写成 `transport.type`，
  内核解码失败退出，Clash API 随之不可用。

### 问题 2：链式代理测速未经过前置节点
- 现象：测速结果不反映链式代理真实延迟。
- 根因：`speedtest.js` 的 `BuildTestNodeTemplate` 里 `delete out['detour']` 主动丢弃
  链式代理字段，且前置节点未加入测试配置，导致测速直连绕过前置节点。

## 二、修复方案

### 修复 A：transport 类型归一（修内核启动）
在 `xhttp`/`splithttp` → `httpupgrade` 做归一，两处兜底：
1. `src/store/uif/parser/uif2singbox.js`（前端最终生成配置处，兜底旧快照）
2. `uifd/subscription/parser/singbox.go` + `clash.go`（后端解析处，新节点不再产生 xhttp）

映射：`xhttp` → `httpupgrade`，`splithttp` → `httpupgrade`，其余（ws/grpc/http/quic/tcp/h2/httpupgrade）不变。

### 修复 B：链式测速（经前置节点）
- `src/uif/template/speedtest.js`：`BuildTestNodeTemplate` 新增第三参数 `resolveDetour`。
  对带 `dial.detour.id` 的节点，把前置节点也加入测试 outbounds（tag 前缀 `detour:`），
  并让被测节点 `detour` 指向前置节点；无法解析时回退直连。
- `src/store/uif/uif.js`：`TestNode` 传入 `resolveDetour = FindOutByID`，
  tag 覆盖循环跳过 `detour:` 前缀的前置节点（前置节点不被测，只作转发）。

## 三、不改动
- 不改 Android 宿主、WT 防火墙、nftables、TUN、`auto_route`、`auto_redirect`。
- 不改订阅 URL、路由 target、出口生成逻辑、探测快照语义。

## 三之二、补充：订阅自动选优作为前置代理（用户追加需求）

需求：订阅设置了前置代理就测速必须加前置；前置是「订阅自动选优」时，
自动选择该订阅中延迟最低的节点作为前置。

- 运行时 `tun_fakeip.js` 的 `InitDetour`：`dial.detour.id` 最后一项是
  `subscription:<id>` 时，把 detour tag 指向该订阅的 urltest 组
  `subscriptionOutboundTag(id)`（自动选最快节点）；无启用节点时退化为空
  （`subscriptionHasEnabledNode` 兜底，避免引用不存在的 tag 导致内核解码失败）。
- 测速 `speedtest.js` 的 `BuildTestNodeTemplate`：新增 `subscription:` 分支，
  把该订阅启用节点加入测试配置并生成同名 urltest 组作为前置；
  成员节点与前置转发节点统一加 `detour:` 前缀，主循环据此跳过上报。
- `uif.js` 新增 `resolveTestDetour` 回调，返回 `{kind:'node'|'subscription',...}`。

### 三之三、可测性重构（把测速 detour 逻辑抽成纯函数，钉死曾写反的分支）

- 新增 `src/store/uif/parser/test_detour.js`：`resolveNodeDetour` / `addTestOutbound`
  纯函数，只依赖 `uif2singbox.js`（Outbound）与 `route_target.js`，均不 import
  全局 uif store。原 `speedtest.js` 内联逻辑迁入，`BuildTestNodeTemplate` 委托调用。
- 修复既有 bug：`speedtest.js` 曾 `import { DEFAULT_IP_DNS } from './tun_fakeip'`，
  但 `tun_fakeip.js` 从不导出 `DEFAULT_IP_DNS`，导致测速配置 DNS 为 `undefined`。
  改为内联 `udp://8.8.8.8`（与 `DEFAULT_REMOTE_IP_DNS` 一致），并移除 `speedtest.js`
  对 `@/utils` 与 `tun_fakeip` 的 import（二者会级联 import 全局 store，jest 下触发
  `Init()→Connect()→Cookies` 副作用导致模板无法单测）。
- 新增 `tests/unit/parser/speedtest_detour.spec.js`（4 用例）：订阅选优前置指向
  urltest 组、无启用节点回退直连、具体节点前置、无法解析回退直连，均通过。

### 三之四、运行时侧订阅选优决策抽纯函数（补齐单测缺口）

- `subscription_detour.js` 新增 `resolveSubscriptionDetourTag(id, hasEnabled)`：
  `dial.detour.id` 最后一项为 `subscription:<id>` 时解析出 urltest 组 tag，
  订阅无启用节点返回 null（回退直连，避免引用不存在的 tag 致内核解码失败）。
- `tun_fakeip.js` 的 `InitDetour` 改为调用该纯函数（原先内联在 `InitDetour` 里
  无法单测）；`SUBSCRIPTION_SELECTOR_PREFIX` 从 `tun_fakeip.js` 的 import 移除
  （不再直接使用）。
- `subscription_detour.spec.js` 新增 4 用例钉死该决策：有启用节点指向 urltest 组、
  无启用节点 null、具体节点 id null、空 id null。

### 三之五、fix7：顺序两段式链式测速，修复全 -1 与竞态

用户复测反馈：「加宽」和「江江」测速都是 `-1`，且前置代理必须先测速；前置如果是订阅，应先选出该订阅最快节点，再作为当前订阅的前置。

根因拆解：

1. 「加宽」的前置代理配置位于订阅级 `subscribe_item_info.dial.detour`，74 个节点本身没有 `node.dial.detour`。旧测速只传节点数组，未传所属订阅对象，因此测速路径没有执行 `InheritSubscriptionDetour`，导致加宽测速仍是直连。
2. 后端 `/delay` 对同一次请求中的所有 tag 并发测速。旧方案把前置订阅 urltest 组与被测节点放进同一个临时配置，实际会出现 urltest 组尚未选出最快前置、下游节点已开始测速的竞态。
3. 前端 20 秒兜底会把仍未返回的节点统一置为 `-1`；一次性测 74/371 个节点时更容易整体超时。
4. 后端错误处理曾把 `err.Error()` 写入 `Msg` 后又无条件用空 `body` 覆盖，导致 UI 只能看到 `-1`，看不到 sing-box Clash API 返回的 `503/504`。

修复：

- `subscribe/item.vue` 调用 `TestNode(this.subscribe_item_info.outbounds, this.subscribe_item_info)`，把所属订阅传入测速函数。
- `uif.js`：
  - `cloneNodesWithSubscriptionDetour` 先把订阅级 detour 继承到被测节点副本；
  - `firstDetourId` 识别当前批次是否需要前置；
  - 前置为 `subscription:<id>` 时，先按该订阅的 `probe.concurrency` 分批测速前置订阅节点，选 `last_probe_delay_ms/delay` 最低的可用节点；
  - 再把这个最快节点作为具体前置节点，顺序分批测速当前订阅；
  - 当前订阅分批大小使用 `probe.concurrency`（默认 4，限制 1~16），避免一次性并发打爆临时 sing-box。
- `test_node.go`：后端 `/delay` 改为 `WaitGroup` 等本批 goroutine 完成后立即返回，不再固定 `sleep 20s`；同时修复错误消息被空 body 覆盖的问题。

真实实测结论：

- `/delay` 后端和临时 sing-box 没有整体损坏：CM 前置样本 8 个里 7 个成功，最快约 409ms。
- 加宽经最快 CM 前置后，前 8 个样本中 5 个成功（约 1262/2427/4813/5312/6625ms），3 个节点本身返回 `503/504`。
- 江江多段抽样大部分节点返回 sing-box Clash API `503/504`，`start=300` 样本有 1 个成功（约 1301ms）。因此江江大量 `-1` 是节点自身不可用/超时，不是 `/delay` 或 UI 误判。

### 三之六、fix8：修复测速大量 `-1` 的剩余逻辑问题

用户复测仍有大量 `-1`，继续查看日志后确认测速逻辑本身还有两个缺陷：

1. 前端 WebSocket 兜底约 25 秒，但后端调用临时 sing-box Clash API 使用通用 `HTTPGetDirect`，外层 HTTP client 超时是 60 秒。日志中出现大量 `1m0s`、`504 Gateway Time-out`、`timeout: no recent network activity`，说明前端会在后端返回前关闭连接，未返回节点被统一标记为 `-1`。
2. `applyProbeResult` 把超过 `threshold_ms` 的成功节点也累计为失败。默认阈值 `200ms` 对代理节点过低，真实可用但较慢的节点会被多次测速后 `quarantined`，后续前置订阅选优和 urltest 都跳过隔离节点，导致可用节点越测越少。

修复：

- `uifd/uif/test_node.go`：
  - `/delay` 请求新增 `timeout_ms`；
  - 新增专用 `getDelayDirect`，Clash API 的 `timeout` 与外层 HTTP client 超时统一为前端传入值（外层仅多 5 秒缓冲），不再被通用 60 秒 HTTP 超时拖住；
  - 临时 sing-box 启动后轮询 `/proxies` 确认 Clash API 就绪，不再固定 sleep 后盲测；
  - 结束临时 sing-box 时调用 `Wait()`，避免僵尸进程堆积；
  - 非 2xx 状态保留 body 和状态码，前端可写入 `probe_error`。
- `src/store/uif/uif.js`：
  - 前端按订阅 `probe.timeout_ms` 等待本批结果，并向后端传 `timeout_ms`；
  - WebSocket 兜底改为 `timeout_ms + 20000`，避免后端仍在正常测试时前端提前标 `-1`；
  - `probeConcurrency` 继续读取订阅 `probe.concurrency`，并把兜底上限改为 5。
- `src/store/uif/parser/subscription.js`：
  - `threshold_ms` 只用于标记 `healthy`/`slow`，成功但慢不再累计失败、不再隔离；
  - 只有真实失败才增加 `consecutive_failures`、触发 `quarantined` 或 `delete`；
  - 失败原因写入 `probe_error`。
- `src/uif_views/outbounds/subscribe/add.vue`：
  - 订阅配置界面新增「测速并发」输入，范围 1~5；
  - 新增「测速超时」输入，范围 3000~120000ms；
  - 阈值说明改为只标记慢速，不作为失败。

fix8 真实 `/delay` 抽样结果：

- CM 前置样本 10 个里 9 个成功，最快约 400ms。
- 加宽经最快 CM 前置后，前 10 个样本中 6 个成功（约 997/1230/1249/2288/4033/4995ms），4 个返回 `503 Service Unavailable`。
- 江江直连多段抽样仍大量失败，但失败原因已明确：`503 Service Unavailable` 或 `504 Gateway Timeout`；`start=300` 样本 8 个里 1 个成功，约 955ms。

### 三之七、fix9：前端不再把未返回节点提前显示为 `-1`

用户继续反馈：UI 仍像是几秒就返回全部结果，很多都是 `-1`，预期应等待后端真正返回才显示。

继续查看日志后确认：内核日志中后端仍在持续测速；前端旧逻辑在 WebSocket `onclose` / `onerror` / 兜底超时后会 `resolve(results)`，上层继续执行，最后把还保持 `" "` 的未返回节点统一标为 `-1`。这会把“没有收到后端结果”伪装成“后端真实失败”。

修复：

- `src/store/uif/uif.js`：
  - 删除 `TestNode()` 末尾的 `markUntestedFailed()` 调用；
  - 删除批量把 `delay == " "` 改成 `-1` 的未返回兜底；
  - WebSocket `onclose` / `onerror` 只提示“未返回节点保持测速中状态”，不写入失败；
  - 前端兜底改为 30 分钟，只防止连接永久挂死，不改变节点状态；
  - 前置订阅/前置节点尚未返回时，不再把当前订阅整批标失败；
  - 回写时同步 `probe_error`，便于展示后端真实失败原因。
- `uifd/uif/test_node.go`：
  - 临时 sing-box Clash API 就绪等待提高到 20 秒；
  - 增加 `delay test start/result/done` 日志，记录每批 tag 数、timeout、每个 tag 的 status/delay/elapsed/msg。

fix9 部署结果：

- 前端产物：`app.7b7f39f8.js`；
- 后端新进程：`uif_service` pid 14811；
- 内核新进程：`sing-box` pid 14849；
- `coreStatus=0`，`coreVersion=1.12.13`；
- 部署脚本已备份旧 service/web，并完成替换。

fix9 真实 `/delay` 受控小批量验证：

- 测试江江 5 个节点，后端日志明确显示：
  - `delay test start: tags=5 timeout_ms=10000`；
  - tag 4 在约 1191ms 返回真实 `503`；
  - tag 1 在约 2029ms 返回真实 `503`；
  - tag 2 在约 3390ms 返回真实 `503`；
  - tag 3 在约 10007ms 返回真实 `504 Timeout`；
  - tag 0 在约 10007ms 返回真实 `504 Timeout`；
  - `delay test done: tags=5 timeout_ms=10000`。
- 因此 fix9 后，如果节点显示 `-1`，应是后端已经针对该 tag 返回真实失败；未收到后端结果的节点不会再被前端批量改成 `-1`。

### 三之八、统一节点选择页测速逻辑

用户确认实际点击的是「节点选择」里的节点测速/测全部延迟，并强调：测速逻辑必须保持一致，必须继承前置代理。

继续排查后确认，「节点选择」页此前走的是另一套 Clash API 逻辑：

- 单节点：`UpdateClashDelay()` → `/proxies/{name}/delay`；
- 全量：`UpdateClashGroupDelay()` → `/group/proxy/delay`；
- 这条路径不会执行 `InheritSubscriptionDetour`，也不会执行“前置订阅先测速选最快，再测目标节点”；
- 旧全量逻辑还会在后端返回前先把所有节点 `delay = -1`。

最终修复：

- `UpdateClashDelay()` / `UpdateClashGroupDelay()` 优先通过 Clash 节点 `name` 映射回 UIF 源节点：
  - 普通手动出站：匹配 `config.outbounds[*].core_tag/tag`；
  - 订阅节点：匹配 `subscribe[*].outbounds[*].core_tag/tag`，同时带回所属订阅对象。
- 能映射回 UIF 源节点的节点，全部调用同一套 `TestNode(sourceNodes, subscriptionInfo)`：
  - 订阅级前置代理先继承；
  - 前置为订阅时，先测前置订阅并选最快具体节点；
  - 再测目标节点；
  - 并发仍按 `probe.concurrency`，代码上限 5；
  - 未返回前保持 loading，不提前写 `-1`。
- 只有无法映射回 UIF 源节点的 Clash 临时项，才走兼容 fallback `/proxies/{name}/delay`。
- 订阅表格 `out_table` 的单节点测速补传 `subscription_info`，避免将来启用该入口时漏继承订阅级前置。
- 按用户要求重新构建源码并整体替换 Droidspaces 内 UIF：前后端一起替换，前端旧目录不备份，清旧 JS/CSS 后解包，避免新旧文件混用。

统一版部署结果：

- 前端产物：`app.eccefff8.js`；
- 后端产物：`uif_service-arm64-unified`；
- 新进程：`uif_service` pid 18963，`sing-box` pid 19002；
- `coreStatus=0`，`coreVersion=1.12.13`；
- 部署后静态检查：前端包含 `__client_timeout_ms`，且不再包含旧 `group/proxy/delay` 路径。

### 验证结果（最终）

- 相关单测 24/24 通过（subscription 8 + route_target 3 + speedtest_detour 4 + subscription_detour 9）。
- 后端 arm64 编译成功并部署，`/delay` 批次按传入超时返回且保留真实错误消息，并写入 `delay test start/result/done` 摘要日志。
- 前端生产构建成功，最终产物 `app.eccefff8.js` 已部署到容器；订阅配置页已支持测速并发 1~5 和测速超时可调；订阅测速和节点选择页测速已统一到同一套链式测速逻辑。
- 产物静态 grep 确认含订阅选优运行时逻辑：`sub::…::urltest` 模板、`subscription:` 前缀判断、`.enabled && !quarantined` 启用节点判断三处齐全。
- 数据流闭环：`out_seletor.vue` 级联产生 `["订阅","<CM-id>","subscription:<CM-id>"]` → `InheritSubscriptionDetour` 继承给「加宽」节点 → `InitDetour` 经 `resolveSubscriptionDetourTag` 指向 `sub::<CM-id>::urltest` → `Bound` 写 sing-box `detour` 字段。测速侧改为先测前置订阅并选最快具体节点，再让当前订阅节点 detour 到该具体前置。

## 四、验证
1. `npx jest`（相关 parser 单测）。
2. `npx vue-cli-service build` 生产构建。
3. 部署后 `coreStatus` 恢复为 0（running），Clash 节点列表恢复。
4. 给订阅配置链式代理后测速，延迟应体现前置节点链路。
